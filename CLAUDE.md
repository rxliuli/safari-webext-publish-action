# Safari WebExtension Publish Action

This is a composite GitHub Action (not a Node.js project). The entire logic lives in `action.yml`.

## What This Does

Signs and uploads Safari web extensions to App Store Connect (macOS + iOS). It auto-detects which platforms to build by reading the Xcode project's schemes.

## When to Use

Add this action as a step in a `macos-26` job, after the Safari Xcode project has been generated (e.g. by `pnpm build:safari` with `wxt-module-safari-xcode`).

## Key Design Decisions

- **Composite action** (not JS/Docker) because it must run on macOS with native tools (xcodebuild, codesign, productbuild, altool)
- **Environment variables for secrets** (not inputs) to match the convention used by `wxt submit` for Chrome/Firefox/Edge
- **Auto-detect platforms** from Xcode schemes rather than requiring a `platforms` input — the Xcode project generated by `safari-web-extension-converter` or `wxt-module-safari-xcode` is the source of truth
- **Archive without signing, then manual codesign** because Xcode's automatic signing doesn't work well in CI without provisioning profiles at archive time
- **macOS uses `productbuild`** (not `xcodebuild -exportArchive`) for the installer package because the "3rd Party Mac Developer Installer" cert works with productbuild but not exportArchive
- **iOS uses `xcodebuild -exportArchive`** because iOS IPA packaging requires it

## Testing Changes

There are no unit tests. To test changes, bump a version in a project that uses this action (e.g. `rxliuli/imp-translate` or `rxliuli/redirector`) and check the CI results.
